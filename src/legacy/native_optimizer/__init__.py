"""Legacy Native Bayesian Optimization Infrastructure (ARCHIVED).

This directory contains the bespoke Gaussian process, acquisition functions, trust region (TuRBO),
multi-objective, and adaptive controller implementations used prior to the BoTorch backend migration.

All active production pipelines, science coordinator layers, and primary benchmarks use BoTorchBackend.
These legacy implementations are preserved strictly for historical reproduction benchmarks and regression audits.
"""

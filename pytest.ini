[pytest]
testpaths = tests
pythonpath = .
addopts = -p no:typeguard
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    benchmark: marks benchmark integration tests
    external_data: marks tests that require external or raw datasets not stored in git
    integration: marks end-to-end integration tests
